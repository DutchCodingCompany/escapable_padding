name: escapable_padding
description: "A very easy way to add padding to widget lists, with the ability to escape the padding for specific widgets."
version: 1.0.0
homepage: https://github.com/DutchCodingCompany/escapable_padding

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^5.1.0

flutter: